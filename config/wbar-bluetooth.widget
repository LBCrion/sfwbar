#Api2

Private {

  include("bluez-popup.widget")

  Var icon = "";

  layout {
    label {
      style = If(BluezAdapter("Count"), "bluetooth", "hidden");
      value = icon + " ";
      trigger = "bluez_adapter";
      action[LeftClick] = Function("XBluezPop");
      action[RightClick] = Menu("XBluezAdapter")
    }
  }
}
