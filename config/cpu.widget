#Api2

include("cpu.source")

export chart "cpu" {
  interval = 1000
  style = If(XCpuPresent, "cpu", "hidden")
  value = XCpuUtilization
  tooltip = "CPU: " + Str(XCpuUtilization*100) + '%'
  action = ExecTerm("top")
}
