include("cpu.source")

layout {
  chart {
    interval = 1000
    style = If(XCpuPresent,"cpu_chart","hidden")
    value = XCpuUsage
    tooltip = "CPU: " + Str(XCpuUsage*100) + '%'
    action = Exec $Term + " -e top"
  }
}
