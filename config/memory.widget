#Api2

include("memory.source")

export scale "memory_meter" {
  interval = 1000
  style = If(XMemPresent, "memory", "hidden")
  value = Str(XMemUtilization, 1)
  tooltip = GT("Memory") + ": " + Str(XMemUtilization*100) + "%"
  action = ExecTerm("top -o %MEM")
}
