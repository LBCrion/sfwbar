#Api2

include("cpu.source")

Private {
  Var icon = '';

  layout {
    label {
      style = if(XCpuPresent,"cpu","hidden")
      interval = 10000
      value = Str(XCpuUtilization*100) + '% ' + icon
    }
  }
}
